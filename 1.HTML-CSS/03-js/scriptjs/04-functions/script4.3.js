function rand10() {
  
  number =  Math.floor((Math.random() * 10) + 1);
}



function multiRand(n) {
  let nA = [];
  n = prompt("Numbers ?");
  if (!isNaN) {
    for (let i = 0; i < n; i++) {
      rand10();
      nA.push(number) 
    }
    console.log(nA);
  } else {alert('Noob')}
}
 multiRand()
